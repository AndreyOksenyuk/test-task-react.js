let initialState = {
   text: 'textRedux'
}

const App_reducer = (state =initialState, action) => {
   switch (action.type) {
   
      default:
         return state
   }
}

export default App_reducer;